services:
  - type: web
    name: farm-backend
    runtime: node
    plan: starter
    dir: .

    buildCommand: |
      npm ci
      npm install -g @nestjs/cli
      npx prisma generate
      npm run build

    startCommand: bash start.sh

    envVars:
      - key: NODE_ENV
        value: production